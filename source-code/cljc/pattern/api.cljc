
(ns pattern.api
    (:require [pattern.core :as core]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; pattern.core
(def ignore!  core/ignore!)
(def reg!     core/reg!)
(def valid?   core/valid?)
(def invalid? core/invalid?)
