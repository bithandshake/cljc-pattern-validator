
(ns patterns.state)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @atom (map)
(def PATTERNS (atom {}))

; @atom (map)
(def TESTS (atom {}))

; @atom (boolean)
(def IGNORED? (atom false))
