
(ns validator.state)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @ignore
;
; @atom (map)
(def PATTERNS (atom {}))

; @ignore
;
; @atom (map)
(def TESTS (atom {}))

; @ignore
;
; @atom (boolean)
(def IGNORED? (atom false))
